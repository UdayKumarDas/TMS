<%if @status == true%>
	// $('#myModal').hide();
	// $('#taskDiv').html("");
	// $('#taskDiv').html('<%= j (render 'projects/view_tasks') %>');
	window.location.href = "<%= j project_path(@project) %>";
<%else%>
	$('#myModal').modal('show');
	$('.modal-body').html('<%= j (render 'tasks/form') %>');
<%end%>